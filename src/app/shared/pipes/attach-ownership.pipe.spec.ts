import { AttachOwnershipPipe } from './attach-ownership.pipe';

describe('AttachOwnershipPipe', () => {
  it('should be defined', () => {
    expect(new AttachOwnershipPipe()).toBeDefined();
  });
});
